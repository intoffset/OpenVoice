[tool.poetry]
name = "openvoice"
version = "0.1.0"
description = ""
authors = ["intoffset <4951782+intoffset@users.noreply.github.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
